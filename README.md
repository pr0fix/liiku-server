## Setup
1. Download HSL-gtfs.zip from: https://api.digitransit.fi/routing-data/v3/finland
2. Extract files to `gtfs-static/` folder
3. Run `npm install`
4. Run `npm run dev`

## Data Attribution
Transit data © Helsinki Regional Transport Authority (HSL)